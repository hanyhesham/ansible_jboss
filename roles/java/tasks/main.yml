---
# tasks file for common

- block:
#  - name: upgrade all packages
#    yum: name=* state=latest

  - name: install java 
    yum:
      name: "{{ java_version }}"
      state: present

  - name: install unzip
    yum:
      name: {{ unzip_version }}
      state: present
  tags: [init]
